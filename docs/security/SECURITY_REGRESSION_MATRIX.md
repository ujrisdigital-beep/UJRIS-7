# Security regression matrix (Step 2A)

Maps Codex Step 1 findings to the Step 2A fix, the automated test that
guards it, residual risk, and the intended Supabase-era replacement.

This matrix does **not** claim production readiness.

| Codex finding | Implementation fix | Automated test | Residual risk | Future Supabase replacement |
|---|---|---|---|---|
| 1. Evidence served as active HTML on the app origin | `buildEvidenceDownloadHeaders`: always `Content-Disposition: attachment`, `X-Content-Type-Options: nosniff`, force `application/octet-stream` for HTML/SVG/JS (declared or sniffed). Path containment in `resolveContainedEvidencePath`. Filename sanitised for `Content-Disposition`. | `tests/security/evidence-delivery.test.ts` | Preview of PDF/images on-origin is still forbidden in this ticket; a future preview feature could reintroduce XSS if inlined. Sniffing only inspects the first 2KB. | Private bucket + signed URLs with `Content-Disposition: attachment` and a non-app download host if preview is required. |
| 2. Authentication lacks rate limits | Interim in-memory limiter (`src/lib/rate-limit.ts`), 5 attempts / 15 minutes, keyed by normalised email, applied in `loginAction` / `signupAction` **before** user lookup. Generic messages. Fail-open if the store throws. **Lazy TTL prune + max-bucket eviction; no global timer.** | `tests/security/rate-limit.test.ts` | Single-process only; resets on restart; not shared across instances; IP rotation still possible; fail-open under store failure. | Supabase Auth rate limits + WAF / edge limiter + shared store. Do not keep this Map. |
| 3. Logout does not revoke captured session tokens | JWT `jti` bound to `AuthSession` row. `verifySessionToken` requires unrevoked, unexpired row. `logoutAction` sets `revokedAt`. Tokens are not stored in plaintext. | `tests/security/session-revocation.test.ts` (capture cookie → 200 → `logoutAction` → **restore captured JWT** → evidence GET 401; session A survives logout of B) | Edge middleware still only checks cookie *presence*. Stolen JWT works until logout/expiry on this instance’s DB. | Supabase Auth session revocation / refresh-token rotation. Drop `AuthSession`. |
| 4. Deadline inference can choose the wrong date and suppress urgency | Strict parser (`parse_status`); event-type allow-list; extraction never `confirmed`; confirmation **span-bound** (source offsets + civil date + event type + rule + **recomputed civil due date**). Same-day distinct events — including two mentions in one sentence — refuse confirmation. Same-date replacement cannot inherit provenance. A second qualifying occurrence with an unresolved / partial / invalid / missing date refuses (`unresolved_qualifying_source`). Legal due dates use pure civil arithmetic (host TZ invariant). `legalClockStatus` ≠ `proceduralUrgency`. Relative dates use an injectable Europe/London clock. Ack ≠ hide / ≠ resolve. | `tests/unit/strict-date.test.ts`, `tests/unit/date-inference.test.ts`, `tests/unit/deadline-confirmation.test.ts`, `tests/unit/civil-date.test.ts`, `tests/unit/civil-date-timezone-invariance.test.ts`, `tests/unit/deadline-attention.test.ts`, `tests/unit/relative-dates.test.ts`, `tests/integration/deadline-confirmation.test.ts`, `tests/integration/deadline-confirmation-invariant.test.ts`, `tests/integration/source-identity.test.ts`, `tests/integration/unresolved-qualifying-source.test.ts`, `tests/integration/deadline-urgency.test.ts`, `tests/integration/procedural-urgency.test.ts`, `tests/integration/hearing-urgency-refresh.test.ts` | Heuristic event typing from prose can still miss UK phrasing. Not legal advice. Hearing tomorrow is high procedural urgency, never a limitation start. SQLite still stores civil dates as DateTime UTC carriers until Postgres DATE in 2B. | Same tests against the future pipeline; store legal dates as DATE; still no LLM deadline arithmetic. |
| 5. Billing fallback grants paid plans when Stripe is absent | Fail-closed: missing Stripe → no paid entitlement. `startCheckoutAction` is the boundary. | `tests/security/billing-action.test.ts`, `tests/unit/billing-mode.test.ts` | If someone sets the flag in a mislabelled non-production deploy, simulated paid access is possible. | Stripe webhooks as sole paid writer; Supabase RLS; remove `activateDevModeSubscription`. |
| 6a. Evidence + custody not atomic | `persistEvidenceWithCustody` uses `db.$transaction`. File deleted only if the evidence row was not committed. | `tests/integration/evidence-integrity.test.ts` | Disk write still happens before the transaction (orphan files possible; not orphan *rows*). SQLite tx is not distributed. | Postgres transaction + storage object in the same workflow (upload → hash → insert → confirm); object delete on rollback. |
| 6b. Hash “verified” merely because a hash exists | `verifyEvidenceIntegrity` recomputes SHA-256 of current bytes vs stored original. Never writes `sha256`. | same file | “verified” is byte-identity, not authenticity. Missing file → `unavailable`. | Same function over object-storage GET; store original hash in an immutable column. |
| 7. Dormant exported helper lacks ownership checks | Server action `refreshCaseIntelligence` requires session; User B cannot refresh or upload into User A's case. | `tests/security/ownership-action.test.ts`, `tests/integration/ownership.test.ts` | Other helpers must keep the same pattern. No RLS yet. | Postgres RLS: `case.user_id = auth.uid()`. Keep the test. |
| 8. No automated tests / CI | Vitest unit/integration/security, Playwright smoke, GitHub Actions **ubuntu-latest + windows-latest** clean-checkout (`npm ci`, typegen, typecheck, lint, tests, **two** `npm run test:e2e`, build) + fail-closed advisory gate. E2E owner is `scripts/e2e-run.mjs` with disposable absolute `prisma/e2e.db`; CI verifies port 4127 is free after each run. npm is never spawned by bare name. Exception matching is **advisory-exact** (no via-name / package-only inheritance). | `docs/testing/CLEAN_CHECKOUT_TESTING.md`, `tests/unit/e2e-lifecycle.test.ts`, `tests/unit/dependency-policy.test.ts`, `tests/unit/command-runner.test.ts`, `tests/unit/audit-execution.test.ts`, `tests/unit/prisma-bootstrap.test.ts`, `tests/unit/database-guard.test.ts` | E2E is smoke-only. Prisma CLI High advisory is a dated **dev_tooling** exception keyed to **GHSA-ggr8-5vv4-36mx** only. Unknown Highs never inherit that exception. Audit ERROR ≠ PASS. Windows proof is the Actions matrix, not a Linux-only clone. | Re-run the same scripts against Supabase test project. |
| 9. Forensic findings lack structured provenance | Design: report/schema/rule-set/pipeline versions, observation raw/normalized/extractor versions, immutable finalized reports, `supersedes_report_id`. Transitional `ForensicFinding` rows for F-TS-001 only. **Worker not built.** | `docs/architecture/FORENSIC_DOCUMENT_INSPECTOR.md`, `tests/integration/forensic-findings.test.ts` | Runtime inspector tables are not implemented. | Implement designed tables after evidence migration. |
| 10. AI outputs lack grounding / cost monitoring | Out of scope (no UJU expansion). | none | Ungrounded phrasing and unbounded LLM cost remain if `OPENAI_API_KEY` is set. | Separate ticket: usage table, grounding checks, budget caps. |
| 11. Reversed timestamp produced incorrect “later” finding | Rule F-TS-001 in `evaluateTimestampAnomaly`: “later revision appears present” only if modified is **after** created by >24h. Reversed pair → `timestamp_inconsistency`. No forged/fraud language. Timezone-equivalent instants produce no finding. | `tests/unit/forensic-timestamps.test.ts` | 24h threshold is a product choice, not a legal standard. EXIF/PDF clocks are often wrong. | Same rule_id on `forensic_findings`; do not reintroduce `Math.abs` for this comparison. |

## R5 safety invariants

**UNRESOLVED QUALIFYING SOURCE INVARIANT:** A deadline may not be confirmed while another potentially qualifying source occurrence exists with an unresolved, partial, ambiguous, invalid or missing date.

**EXCEPTION IDENTITY INVARIANT:** A dependency exception applies only to the exact approved advisory identity and approved dependency context. Unknown or different High advisories never inherit another advisory's exception.

## Interim controls (must not be described as production-grade)

- In-memory auth rate limiter
- Custom JWT + `AuthSession` revocation
- Local disk evidence storage
- SQLite (no RLS)
- Optional non-production billing simulation flag
